// public/script.js (Diagnostic Version - Minimal)

// 在文档加载完成后立即执行
document.addEventListener("DOMContentLoaded", () => {
    // 在浏览器的开发者控制台打印一条消息，证明脚本被执行
    console.log("诊断脚本 V2 已成功加载并执行！");

    // 修改页面的标题，这是一个肉眼可见的变化
    document.title = "【诊断成功】JS已运行";
});